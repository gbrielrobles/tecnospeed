{
  "name": "tecnospeed",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start:dev": "concurrently \" cd backend && docker compose up -d\" \"cd frontend && yarn run dev\"",
    "install:dependecy": "concurrently \" cd backend && yarn install \" \" cd frontend && yarn install\""
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^22.15.30",
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "class-transformer": "^0.5.1",
    "pdfkit": "^0.17.1",
    "prisma": "^6.5.0"
  }
}
